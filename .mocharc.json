{
  "require": ["ts-node/register"],
  "extensions": ["ts"],
  "spec": "tests/**/*.test.ts",
  "timeout": 30000,
  "reporter": "spec",
  "bail": false,
  "exit": true,
  "recursive": true,
  "loader": "ts-node/esm"
} 